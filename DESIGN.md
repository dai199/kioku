# Kioku デザイン言語

方針: **macOSネイティブに溶け込む**（SPEC §8）。Apple HIG と macOS 26 の Liquid Glass に従い、
独自色は出さない。すべての画面はこの文書のトークンに従う。

参照: [Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
（特に The menu bar / Panels / Materials / Typography / Motion）

## タイポグラフィ階層

コンテンツの重要度で3層に分ける。色ではなくサイズで主役を示し、色は補助に使う。

| 層 | フォント | 色 | 用途 |
|---|---|---|---|
| 主役 | `.title3` | primary | 訳文、復習カードの英文 |
| 文脈 | `.callout` | primary | 原文、履歴の本文 |
| メタ | `.caption2` / `.caption` | `.secondary` / `.tertiary` | アプリ名、日時、バッジ、操作ヒント |

## 余白・形状

- パディング: **12pt**（ポップアップ内）、**14pt**（ウィンドウ内のコンテンツ）
- 要素間スペース: 8pt（セクション間）、6pt（行内）
- 角丸: **14pt**（ポップアップ）、8pt（カード・リスト内ボックス）
- 背景: `.regularMaterial` ＋ `.separator` の0.5ptボーダー。ベタ塗りの背景色は使わない
- サイズは**内容にフィット**させる。固定サイズ＋余白埋めはアンチパターン（HIG: パネルは必要最小限）

## シンボル・アイコン

- アプリのアイデンティティは **`translate`** シンボル（メニューバー・フローティングアイコン共通）
- メニューバーはモノクロのテンプレートレンダリング（色反転はOSに任せる）
- 状態異常は `exclamationmark.triangle.fill`
- メニュー項目にはSF Symbolsを添える（システムメニューの慣例に合わせる）

## モーション

- 出現: フェード 0.12〜0.15s ＋ わずかなスケール（0.97→1.0、anchor は成長方向に合わせる）
- ポップアップの伸縮: 上端固定で下方向に成長（選択テキストとの位置関係を守る）
- 消滅はアニメーションなしで即座に（transientなUIはすぐ消えるべき）

## ポップアップ固有のルール

- **nonactivating panel**: 前面アプリのフォーカスを絶対に奪わない（本文置換の前提条件）
- クリック操作は `Button` で実装する。`onTapGesture` は非キーウィンドウでクリックを取りこぼす
- テキスト省略は `lineLimit` ではなく**手動切り詰め**（システムの「…クリックで全文オーバーレイ」が
  下の内容にかぶるのを防ぐ）。展開はポップアップ自体を下に伸ばして行う
- 画面端では自動で位置補正し、全体が必ず見えることを保証する
- 吹き出し矢印は選択範囲を指す（下向き配置に収まらず上に反転した場合は矢印なし）

## 色

- セマンティックカラーのみ使用（`.primary` / `.secondary` / `.tertiary` / `.separator` / `.quaternary`）
- アクセントはシステムのアクセントカラーに委ねる（`borderedProminent` / `.tint`）
- 独自の色定義はしない。ダークモード対応はセマンティックカラーとMaterialで自動的に成立させる
