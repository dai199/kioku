# Kioku デザイン言語

方針: **macOSネイティブに溶け込む**（SPEC §8）。Apple HIG と macOS 26 の Liquid Glass に従い、
独自色は出さない。すべての画面はこの文書のトークンに従う。

参照: [Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
（特に The menu bar / Panels / Materials / Typography / Motion）

## タイポグラフィ階層

コンテンツの重要度で3層に分ける。色ではなくサイズで主役を示し、色は補助に使う。

| 層 | フォント | 色 | 用途 |
|---|---|---|---|
| 主役 | `.title3` | primary | 訳文、復習カードの英文 |
| 文脈 | `.callout` | primary（従属側は `.secondary`） | 原文、履歴の本文、カード候補 |
| メタ | `.caption2` | `.secondary` / `.tertiary` | アプリ名、日時、バッジ、操作ヒント |

例外: **復習画面**は全画面フォーカスの学習の瞬間なので、答え（英文）に `.title2` まで許容する。

## ウィンドウ構成

- **メインウィンドウ**（`MainWindow.swift`）— 復習・翻訳履歴・週次レポートを
  `NavigationSplitView` のサイドバーで切り替える1枚。学習データを見る画面はここに集約する
  （Mail・Music と同じmacOS標準の型。相互に行き来する画面をウィンドウで散らかさない）
  - サイズ: 760×540（最小 620×420）。サイドバーは**固定幅180pt**（ドラッグでの幅変更はしない）
  - サイドバーの開閉は**ツールバー末尾の自前ボタン**で行う。`NavigationSplitView` 組み込みの
    開閉ボタンはカラムに追従して位置が動くため使わない（`.toolbar(removing: .sidebarToggle)`）。
    ウィンドウ座標に固定される場所に操作を置くのが原則
  - タイトルは選択中のセクション名
  - 復習セクションは開くたび・切り替えるたびにセッションを仕切り直す。
    履歴とレポートのDB監視は張りっぱなしにする（`MainWindowModel` がモデルを保持）
- **設定ウィンドウ**（`SettingsWindow.swift`）— ⌘, で開く独立ウィンドウ。
  HIGの慣習に加え、ポップアップの歯車からも開くため、メインウィンドウには含めない
- **ポップアップ**（`PopupController.swift`）— 下記「ポップアップ固有のルール」に従う

## 共通コンポーネント（DesignComponents.swift）

- `DirectionBadge(sourceLang:)` — 翻訳方向バッジ。方向表示は必ずこれを使う
- `.cardBox()` — カード状ボックス（角丸8pt・quaternary背景・パディング10pt）。
  レポートのパターン/カード候補などリスト内の塊はこれで統一する

## 余白・形状

- パディング: **12pt**（ポップアップ内）、**14pt**（ウィンドウ内のコンテンツ）
- 要素間スペース: 8pt（セクション間）、6pt（行内）
- 角丸: **14pt**（ポップアップ）、8pt（カード・リスト内ボックス）
- 背景: `.regularMaterial` ＋ `.separator` の0.5ptボーダー。ベタ塗りの背景色は使わない
- ポップアップ・パネルのサイズは**内容にフィット**させる。固定サイズ＋余白埋めはアンチパターン
  （HIG: パネルは必要最小限）
- メインウィンドウはリサイズ可能。中身は幅いっぱいに広げず、読みやすい幅で止める
  （復習カードは最大460pt・中央寄せ）

## シンボル・アイコン

- アプリのアイデンティティは **`translate`** シンボル（メニューバー・フローティングアイコン共通）
- メニューバーはモノクロのテンプレートレンダリング（色反転はOSに任せる）
- 状態異常は `exclamationmark.triangle.fill`
- メニュー項目にはSF Symbolsを添える（システムメニューの慣例に合わせる）

## モーション

- 出現: フェード 0.12〜0.15s ＋ わずかなスケール（0.97→1.0、anchor は成長方向に合わせる）
- ポップアップの伸縮: 上端固定で下方向に成長（選択テキストとの位置関係を守る）。
  **この伸縮はアニメーションさせない**（SwiftUI側でもウィンドウ側でも）。
  内容にフィットするパネルは「中身の高さ → ジオメトリ通知 → ウィンドウのリサイズ」で
  追従しているので、途中に中間サイズを作るとそれが通知として戻ってきてループになる。
  上端の基準もその都度ずれ、ウィンドウ全体が揺れる。動かしたいときは
  サイズではなく中身の不透明度で（ジオメトリに触らない）
- 消滅はアニメーションなしで即座に（transientなUIはすぐ消えるべき）

## ポップアップ固有のルール

- **nonactivating panel**: 前面アプリのフォーカスを絶対に奪わない（本文置換の前提条件）
- クリック操作は `Button` で実装する。`onTapGesture` は非キーウィンドウでクリックを取りこぼす
- テキスト省略は `lineLimit` ではなく**手動切り詰め**（システムの「…クリックで全文オーバーレイ」が
  下の内容にかぶるのを防ぐ）。展開はポップアップ自体を下に伸ばして行う
- 読み上げボタンは**その文の右**に置く（原文・訳文で対称）。下のボタン列は
  「訳をどうするか」の操作だけに絞る。幅360ptに並ぶ数を増やさないための線引き
- 展開して伸びるものは**ボタン列より下**に置く。上に置くとボタン列が押し下げられ、
  押したいボタンが動く
- **生成を伴う操作は、開閉ではなく生成だと分かる形にする**。押す前は ✨ で示し、
  押した後は経過秒を出す（「詳しく」がこれ）。ただの開閉に見せると、
  待たされたときに壊れたと受け取られる
- 画面端では自動で位置補正し、全体が必ず見えることを保証する
- 吹き出し矢印は選択範囲を指す（下向き配置に収まらず上に反転した場合は矢印なし）

## 色

- セマンティックカラーのみ使用（`.primary` / `.secondary` / `.tertiary` / `.separator` / `.quaternary`）
- アクセントはシステムのアクセントカラーに委ねる（`borderedProminent` / `.tint`）
- 独自の色定義はしない。ダークモード対応はセマンティックカラーとMaterialで自動的に成立させる
