# Kioku — 言語学習者向け翻訳アプリ 要件仕様書

作成日: 2026-07-04（インタビュー形式で要件確定）

## 1. ビジョン

翻訳アプリであると同時に学習アプリである。macOS上のあらゆるアプリでテキストを選択して翻訳でき、その翻訳履歴が自動的に学習素材として蓄積される。週次でAIが「どの英文を覚えれば効率的か」をレコメンドし、間隔反復（SRS）で定着させる。

Grammarlyに近い体験だが、「過去ログの見やすさ」と「何を覚えるべきかの提示」を強化した学習特化版。

## 2. ターゲット

- **メインユーザー**: 日本人の英語学習者（開発者自身がユーザー第一号）
- **言語ペア**: 英⇔日に絞る（多言語化は将来の検討事項）
- **プラットフォーム**: macOS先行。将来的にWindows・モバイル（復習体験はモバイルとの相性が良い）

## 3. コア機能

### 3.1 テキスト選択 → ポップアップ翻訳（システムワイド）

- **範囲**: macOS全体。どのアプリでも（Safari、Slack、PDF、エディタ等）テキスト選択に反応する
- **発動UX**: 選択すると小さなフローティングアイコンが出現 → クリックで翻訳ポップアップが開く（PopClip方式）
  - 選択しただけでは何も送信されない（プライバシー方針と一体）
- **言語自動判定**: 選択テキストが英文なら英→日、日本語文なら日→英として動作

### 3.2 英→日モード（読解支援）

- ポップアップに**自然な日本語訳のみ**を即表示（高速・低コスト重視）
- **TTS読み上げボタン**（原文英語の発音確認、AVSpeechSynthesizerで無料実装）
- 詳細解説はポップアップでは出さず、履歴画面で後から見る設計

### 3.3 日→英モード（ライティング支援・本文置換）

- 日本語を選択 → ポップアップに英訳候補を表示
- **採用**: 候補をクリックすると選択中の日本語が英文に**置き換わる**
- **再生成**: 候補の近くのボタンで別の英訳を生成し、選び直せる
- **置換の実装**（二段構え＋フォールバック）:
  1. Accessibility APIの書き込み（対応アプリ）
  2. ダメならペーストシミュレート（⌘V相当）
  3. 読取専用領域では置換ボタンの代わりに**コピーボタン**を表示

### 3.4 翻訳履歴

- macOSアプリ内の履歴画面（メニューバーから開くウィンドウ）
- **保存データ**:
  - 選択文＋訳文＋日時（必須）
  - 出典: アプリ名・URL・ページタイトル（Accessibility経由で取得）
  - 日→英モードでは「元の日本語＋採用した英文」のペア
- 将来: Webダッシュボード（フェーズ2、同期サーバー実装後）

### 3.5 学習レコメンド（差別化の核）

方式は**頻出パターン抽出型 × SRS型**のハイブリッド:

- **週次レポート**（macOS通知でプッシュ）:
  - 今週の翻訳サマリー（件数・出典別内訳）
  - AIが翻訳ログから抽出した「繰り返しつまずいている文法・語彙パターン」
- **SRSカード化**: AIが週次でログを分析し「覚える価値の高いN件」を選別してカード化を提案 → ユーザーはワンタップ承認するだけ（全自動カード化によるカード溢れを防ぐ）
  - 日→英ログはカードの素材として最適（表: 日本語 / 裏: 採用英文）

### 3.6 SRS復習

- **操作**: カードをめくって「ダメ / まあまあ / 余裕」の3段階自己評価
- **アルゴリズム**: FSRS系の最新スケジューリング
- **負荷設計**: 1回の復習セッション5分以内目標、1日20枚程度を上限
- 復習体験は将来的にモバイルが本命（フェーズ3）

## 4. 技術スタック

| 領域 | 選定 | 備考 |
|---|---|---|
| アプリ | Swift / SwiftUI フルネイティブ | メニューバー常駐アプリ |
| 対応OS | macOS 26 (Tahoe) 以降のみ | 自分用MVPのため最新のみ。公開時に引き下げ検討 |
| 選択検知 | Accessibility API | 権限付与が必要。App Storeサンドボックス非互換 |
| ローカルDB | SQLite（GRDB推奨） | ローカルファースト。将来のクロスプラットフォーム同期を見据えスキーマは言語ペアを抽象化して設計 |
| TTS | AVSpeechSynthesizer | 無料・オフライン |

### 4.1 AI構成（二層＋抽象化）

- **`TranslationEngine`プロトコルで翻訳エンジンを抽象化**（将来の差し替え・プロバイダ追加を容易に）
- **日常の翻訳**: クラウド最安クラス（Gemini Flash-Lite級）。1翻訳0.01円未満なのでコストは実質無視できる
- **週次分析・レコメンド生成**: 上位モデル（Gemini Flash/Pro級）。週1回なのでコスト影響小
- **Apple Translation framework**: オフライン時のフォールバックとして組込み
- **Foundation Models（オンデバイス）**: 品質が実用域に達したら翻訳エンジンの選択肢に追加（抽象化済みなので差し替え容易）
- **週次レポート生成**: ローカルでスケジュール実行し、BYOKで上位モデルを直接呼ぶ（サーバー不要）

### 4.2 APIキー・課金アーキテクチャ

- **MVP**: BYOK（ユーザー自身のGemini APIキーを設定）。サーバー完全不要
- **SaaS化時**: 認証＋APIプロキシサーバーを追加してサブスク課金。OSSセルフホスト派はBYOKのまま使い続けられる

## 5. プライバシー

- テキスト送信は**常に明示的操作（アイコンクリック）の後のみ**。選択しただけでは何も送らない
- **アプリ除外リスト**: パスワードマネージャー・銀行アプリ等ではアイコン自体を表示しない
- ログはローカルDBに保存（ローカルファースト）

## 6. 配布・ビジネスモデル

- **MVP**: 自分用ビルド（署名のみ）。配布方法は公開時に決定
  - 注: Accessibility API必須のためMac App Store サンドボックスとは非互換。公開時はnotarize済み直接配布（＋Sparkle自動更新）が有力
- **ライセンス**: 未決。クローズドで開始し公開時に判断（非公開→公開は可逆、逆は不可逆）
  - 参考モデルA: 37signals式 = SaaSクローズド＋セルフホスト版は買い切り/無料でソース付き
  - 参考モデルB: WordPress式 = AGPLで全公開、自分がSaaSを優位に運営
- **収益構想**: セルフホスト（BYOK・無料or買い切り）と、ホスト版SaaS（サブスク）の二本立て

## 7. データモデル（草案）

```
TranslationLog
  id, createdAt
  sourceText, translatedText
  sourceLang, targetLang          -- 言語ペアは最初から抽象化
  direction (reading | writing)   -- 英→日読解 / 日→英ライティング
  sourceApp, sourceURL, sourceTitle

SRSCard
  id, logId (FK), createdAt
  front, back                     -- 例: 日本語 / 英文
  origin (ai_suggested | manual)
  fsrsState (stability, difficulty, dueDate, ...)

ReviewLog
  cardId, reviewedAt, rating (again | good | easy)

WeeklyReport
  id, weekOf, generatedAt
  summaryJSON                     -- サマリー・頻出パターン・カード提案
```

## 8. デザイン方針

- **macOSネイティブに溶け込む**: Liquid Glass・SF Symbolsなどシステム標準に従い、OSの一部のような佇まい
- ポップアップは即応感・非侵入性を最優先
- デザインモックはClaude（Artifact/デザインツール）で作成してから実装に入る

## 9. ロードマップ

### フェーズ1: MVP（macOS、自分用）— ✅ 完了（2026-07-06）
1. ✅ メニューバー常駐アプリの骨格＋Accessibility権限フロー
2. ✅ テキスト選択検知 → フローティングアイコン → ポップアップ（マウス＋キーボード選択対応、URL/数字等はフィルタ、スペースをまたがない）
3. ✅ 英→日翻訳表示（Gemini BYOK）＋TTS
4. ✅ 日→英翻訳＋本文置換（AX書込→検証→ペーストフォールバック→コピー）＋再生成
5. ✅ ローカルDBへのログ保存（GRDB）＋履歴画面（検索・削除・自動更新）
6. ✅ 週次レポート生成（自動チェック＋手動）＋通知＋SRSカード提案・承認
7. ✅ SRS復習画面（3段階評価・1日20枚上限・「ダメ」は同セッション再出題）
   - 注: スケジューラは現状SM-2系の簡易実装（`ReviewScheduler`）。本格FSRSへは同型の差し替えで対応予定

### フェーズ1.5: 使い込みながらの改善（バックログ）

優先度順。⭐は「スタイル適応（§11）・品質評価（§12）の前提となるシグナル収集」。
番号は着手順の記録として維持する（完了しても詰め直さない）。

1. ✅ ⭐ **選好シグナルの収集開始**: 「別の訳」で却下した候補・再生成回数・何番目の候補を採用したかをDBに記録する（§11・§12の学習データ。早く始めるほど価値が出る）
   - `adoptedVia` は replace（AX書き込み・反映を確認済み）/ paste（⌘V送信・反映は未確認）/ copy の3値。
     採用率を測るときpasteはreplaceと同列に扱わないこと（反映されたか分からないため）
2. ✅ **翻訳のストリーミング表示＋キャッシュ**: 体感速度の改善。同一テキストの再翻訳はローカルキャッシュから即返す
3. ✅ **手動カード追加**: ポップアップと履歴画面から「覚える」。AI提案を待たずにカード化（習慣ループの強化）
   - AI提案と違い承認を挟まず、はじめから active・当日期限で作る
   - 表裏は方向によらず「表=日本語・裏=英文」に揃える（`CardContent`）
   - 同じ表裏のカードは重複追加しない。履歴側はカード化済みの行に印を出す
4. **単語・熟語モード**: 1〜3語の短い選択では訳だけでなく語義・品詞・例文・類語を表示（辞書的UI）
   - 2026-08-05: 優先度を下げた。本当に必要かの判断がついていない
     （辞書アプリで足りる可能性がある）。使い込んで「欲しい」と思ってから着手する
5. **ポップアップの「詳しく」展開**: 文法解説・ニュアンス解説を追加生成（初期設計で見送った段階表示の復活）
6. **本格FSRS導入**: `ReviewScheduler`の差し替え（現行SM-2系の挙動は`ReviewSchedulerTests`に固定済み。差し替え前後の差分はここで見る）
7. ✅ **復習リマインダー通知**: 毎朝8時、期限到来カードの枚数を通知（`ReviewReminder`）
   - 本文に当日の枚数を入れたいので、OSのカレンダートリガーではなく30分ごとの自前チェック
   - 今日のぶんを終えている／期限カードが0枚なら黙る。朝を逃してもその日のうちに1回鳴る
   - 時刻を設定可能にするかは使ってみてから判断する（現状は8時固定・オン/オフのみ設定可）
8. ✅ **週次レポートの過去一覧**: 生成履歴を遡って閲覧（ツールバーの週メニュー＋週送り）
9. ✅ **ユニットテスト**: `Tests/KiokuTests`（`make test`）。SelectionFilter・LanguageDetector・ReviewScheduler・WeeklyAnalyzerのJSON抽出をカバー
   - AX API・ネットワーク・DBに触れる層は未カバー。次に伸ばすならDB層（マイグレーションと`dueCards`の境界）
10. **ブラウザURL出典の正確な取得**: AppleScript連携（Chrome/Safari）でページURLを記録
11. **自動起動（ログイン項目）・履歴エクスポート（CSV/Anki）** ／ アプリ除外リストは ✅
    - ✅ **アプリ除外リスト**: 設定画面から.appを選んで追加する（バンドルIDの手入力はさせない）。
      初回起動時にパスワードマネージャー数種を既定で入れる。除外アプリでは
      アイコンを出さず、選択を記憶もしない（§5: 送信操作をする余地自体を作らない）
    - 自動起動・エクスポートは未着手
12. **アップデートの有無をチェックする**（2026-08-05 追加）
    - 段階1: GitHub ReleasesのAPIで最新タグを見て、新しければ知らせるだけ（依存なし・鍵管理なし）
    - 段階2: Sparkleで自動更新（appcast＋EdDSA署名）。§6の配布方針と一体で決める
    - 前提: 自動インストールまで行くならnotarize（＝Hardened Runtime有効化）が要る。
      現状は `ENABLE_HARDENED_RUNTIME: false` の自分用署名なので、
      フェーズ2の「配布方法決定」が先に来る

### フェーズ2: 公開準備
- 配布方法決定（notarize直接配布が有力）・ライセンス決定
- アプリ除外リスト等プライバシー機能の拡充
- Webダッシュボード＋同期サーバー（SaaS基盤）

### フェーズ3: 展開
- 認証＋APIプロキシ＋サブスク課金（SaaS）
- モバイル版（復習体験の本命）
- Windows版

## 10. 未決事項

- 配布方法・ライセンスの最終決定（公開時）
- 週次レポートの生成曜日・時刻（設定可能にし、デフォルトは日曜朝を想定）
- 同期プロトコルの設計（フェーズ2）
- **無料枠APIの扱い**（2026-08-05）: Geminiの無料枠は送信内容が製品改善に使われうる
  規約と理解している（有料枠は対象外）。要確認。事実なら§5のプライバシー方針と
  一貫しないため、公開時に設定画面で明示するか、有料枠を前提にするかを決める必要がある。
  あわせて無料枠は応答が15〜20秒かかることがあり（実測・通信経路は健全）、
  体験面でも有料枠との差が大きい
- 多言語ペア対応の時期
- Windows版の技術選定（ロジック仕様を共有して別実装）

## 11. スタイル適応（構想）

「よりカジュアルに・より柔らかく」といった軸で、ユーザー好みの訳が最初から出やすくする。

### スタイル軸（2026-08-05に2本へ絞った）
- **フォーマリティ**: カジュアル ↔ フォーマル
- **長さ**: 簡潔 ↔ 丁寧・冗長

当初案にあった「トーン（柔らかい↔直接的）」「語彙レベル（平易↔洗練）」は落とした。
どちらもフォーマリティと強く相関し（英語では「丁寧＝遠回し」がほぼ同義）、
軸を増やすと同じ傾向が分散して推定が弱くなるだけだったため。
長さを残したのは、**文字数で機械的に測れて推論が要らない**という点で他と性質が違うから。

将来足すなら「直截さ（hedgingの量）」「専門用語を英語のまま残すか」が候補。
どちらもフォーマリティと独立している。

### 効くのは日→英
採用の意思表示（置き換える／コピー）があるのは実質日→英だけで、英→日は
表示した時点でログに残るのみ。訳文の文体が他人の目に触れるのも日→英なので、
スタイル適応はこちらに集中する。

### 学習の仕組み
1. ✅ **シグナル**: 「別の訳」で却下した候補と採用した候補のペア（フェーズ1.5-1）
2. ✅ **方向つき再生成**: 「もっとカジュアルに／もっとフォーマルに／もっと短く」を
   ユーザーに選ばせ、`translationSession.styleDirectionsJSON` に要求順で残す。
   素の「別の訳」は却下理由（文体か誤訳か）が分からないが、方向指定は**推論ではなく事実**。
   AIの判断を挟まずにプロファイルへ反映できるので、ここが学習データの本命
3. **プロファイル更新**: 週次分析で方向指定の履歴を集計し、軸の値を推定・更新
   （方向指定が足りない間は採用/却下ペアからの推定で補う）
4. **適用**: 翻訳プロンプトに「カジュアル寄り（0.7）」のように注入
   - 注: `TranslationCache` のキーと `TranslationSessionRecord.promptVersion` に
     プロファイル版を含めること。含めないと、プロファイルを変えても
     古い訳がキャッシュから返り、「どの設定で出た訳か」も追えなくなる
5. **手動オーバーライド**: 設定にスライダー。各軸に自動/手動の切り替えと、
   **推定の根拠件数**を出す（3件からの推定と200件からの推定を同じ見た目にしない）。
   推定値を見せずに効かせると、訳が悪化したときユーザーが理由に辿り着けない
6. **アプリ別プロファイル**（発展）: Slackはカジュアル、メールはフォーマル

## 12. 翻訳品質の評価と改善（構想）

「良い翻訳」の基準を定義してスコア化し、プロンプト変更の良し悪しを測定可能にする。

### 品質ルーブリック（案）
1. **意味の忠実性**: 原文の意味・情報が欠落/歪曲していないか
2. **自然さ**: ネイティブが書いたように読めるか（翻訳調でないか）
3. **レジスタ一致**: 原文のトーン・場面（出典アプリ）に合った文体か
4. **一貫性**: 同じ用語・言い回しが揺れないか

### 測定方法（組み合わせる）
- **LLM-as-judge**: 上位モデルがルーブリック各項目を1〜5で採点（バッチ・オフライン評価）
- **行動シグナル**: 再生成率（=1回で満足できなかった率）、採用までの候補数、置換後の手直しの有無
- **逆翻訳チェック**: 訳文を原言語に戻して意味の一致度を測る（自動回帰テスト向き）

### 改善サイクル
1. ユーザーが「良い」とした翻訳ペアを**ゴールデンセット**として蓄積（履歴に👍ボタン等）
2. プロンプト変更はゴールデンセット＋judge採点で回帰テストしてから採用（スコアが下がる変更は入れない）
3. プロンプトはバージョン管理し、翻訳ログに使用プロンプトverを記録（どの変更が効いたか追跡可能に）

